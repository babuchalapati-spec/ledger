{
  "name": "ledger-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Ledger Records Android companion app",
  "scripts": {
    "sync": "cap sync android",
    "build-apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
